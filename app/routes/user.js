import Ember from 'ember';

export default Ember.Route.extend({
  // model(params) {
  //   let token = '2f1da9d5573708e5853735514f2e89f45bb4c04f';
  //   let url = `https://api.github.com/users/${params.username}?access_token=${token}`;
  //   return Ember.$.getJSON(url);
  // }
});
